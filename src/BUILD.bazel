load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "main",
    srcs = ["main.rs"],
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:rustls",
        "@crates//:tracing",
        "@crates//:tracing-subscriber",
    ],
)
